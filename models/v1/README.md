# V1 model registry

No model artifact is checked in. Supply a validated `ensemble/model.onnx`, `metadata.json`, `calibration.json`, and schema-compatible OOD metadata only after running the chronological training/evaluation gates. The Android application intentionally abstains while this directory has no trained artifact.
